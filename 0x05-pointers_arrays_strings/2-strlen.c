#include "main.h"
/**
  *' _strlen' : function name
  * @s: function parameter
  * Return: the lenght of the string
  */

int _strlen(char *s)
{
int i;
int count = 0;

for (i = 0; s[i] != *int i++;)
	count++;
return (count);
}
